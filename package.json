{
  "name": "nexus",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "db:migrate": "pnpm -r db:migrate"
  },
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "@prisma/client",
      "@prisma/engines",
      "prisma"
    ]
  }
}
