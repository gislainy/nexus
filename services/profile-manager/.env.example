# Database
DATABASE_URL=postgresql://nexus:nexus@localhost:5432/nexus

# Server
PORT=8002

# Auth — same secret used by services/auth/ to sign the tokens
JWT_SECRET=

# Shared key for service-to-service calls (C3 → C1)
SERVICE_API_KEY=
